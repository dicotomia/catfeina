/**
 * Gestión del espacio de coworking y arranque de componentes compartidos.
 */

import { inicializarEstructuraCarrito } from '../recursos/js/carrito.js';

document.addEventListener('DOMContentLoaded', () => {
    inicializarVistaCoworking();
    inicializarEstructuraCarrito();
});

/**
 * Inicializa las funcionalidades específicas para la sección de coworking.
 */
function inicializarVistaCoworking() {
    // La interactividad se gestiona mediante CSS y el sistema de reservas global.
}
